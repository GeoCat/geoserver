<?xml version="1.0" encoding="ISO-8859-1"?>
<!-- 
  Copyright (C) 2019 - GetCat B.V. All rights reserved.
  Copyright (C) 2014 - Open Source Geospatial Foundation. All rights reserved.
  This code is licensed under the GPL 2.0 license, available at the root
  application directory.
 -->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>net.geocat.enterprise</groupId>    
    <artifactId>geoserver</artifactId>
    <version>2.16-SNAPSHOT</version>
  </parent>

  <groupId>net.geocat.enterprise.geoserver</groupId>
  <artifactId>gs-enterprise-webapp</artifactId>
  <packaging>war</packaging>
  <version>2.16-SNAPSHOT</version>
  <name>GeoServer Enterprise Application</name>
  <description>
     GeoServer Web Application for GeoCat distribution. 
     
     Web Application used for enterprise branding, with profiles selecting modules to prepackage for GeoCat Live hosting and GeoCat customers.
  </description>
  
  <dependencies>
    <!-- GeoServer Enterprise modules -->
    <dependency>
      <groupId>net.geocat.enterprise.geoserver</groupId>
      <artifactId>gs-enterprise-theme</artifactId>
      <version>${project.version}</version>
    </dependency>

    <!-- GeoServer modules -->
    <dependency>
      <groupId>org.geoserver.web</groupId>
      <artifactId>gs-web-app</artifactId>
    </dependency>
    
    <!-- GeoServer community plugins -->
    <dependency>
      <groupId>org.geoserver.community</groupId>
      <artifactId>gs-web-resource</artifactId>
      <version>${gs.version}</version>
    </dependency>
  </dependencies>

  <build>
    <finalName>geoserver-enterprise</finalName>
    <resources>
      <resource>
        <directory>src/main/resources</directory>
      </resource>
    </resources>
    <plugins>
      <!-- Builds a valid data directory into the web app -->
      <!--plugin>
        <artifactId>maven-antrun-plugin</artifactId>
        <version>1.7</version>
        <executions>
          <execution>
            <id>configPackage</id>
            <phase>package</phase>
            <configuration>
              <tasks>
  <!- check if config is available, if not fail ->
  <available file="${configDirectory}/${configId}" property="configIsAvailable" />
  <fail message="Unable to locate configuration: ${configDirectory}/${configId}" unless="configIsAvailable" />

  <!- copy the files from the config dir to the webapp dir ->
  <echo message="Copying data dir from ${configDirectory}/${configId}" />
  <copy todir="${webappSourceDirectory}/data">
     <fileset dir="${configDirectory}/${configId}" includes="**/**" />
  </copy>
              </tasks>
            </configuration>
            <goals>
              <goal>run</goal>
            </goals>
          </execution>
          <execution>
            <id>configClean</id>
            <phase>clean</phase>
            <configuration>
              <tasks>
  <delete dir="${webappSourceDirectory}/data" />
              </tasks>
            </configuration>
            <goals>
              <goal>run</goal>
            </goals>
          </execution>
        </executions>
      </plugin-->
      <plugin>
        <artifactId>maven-war-plugin</artifactId>
        <version>3.0.0</version>
        <configuration>
          <packagingExcludes>WEB-INF/lib/gs-web-app-*.jar</packagingExcludes>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.eclipse.jetty</groupId>
        <artifactId>jetty-maven-plugin</artifactId>
        <version>${jetty.version}</version>
        <configuration>
          <webApp>
            <contextPath>/geoserver</contextPath>
          </webApp>
          <connectors>
            <connector implementation="org.mortbay.jetty.nio.SelectChannelConnector">
              <port>${jetty.port}</port>
              <maxIdleTime>10000</maxIdleTime>
            </connector>
          </connectors>
          <supportedPackagings>
            <supportedPackaging>war</supportedPackaging>
          </supportedPackagings>
          <stopPort>${jetty.stopPort}</stopPort>
          <stopKey>GEOSERVER_JETTY_STOPKEY</stopKey>
          <daemon>false</daemon>
          <contextXml>${project.build.testOutputDirectory}/jetty-context.xml</contextXml>
        </configuration>
      </plugin>
      <!--plugin>
          <groupId>org.codehaus.mojo</groupId>
          <artifactId>build-helper-maven-plugin</artifactId>
          <version>1.8</version>
          <executions>
              <execution>
                  <id>attach-war</id>
                  <phase>package</phase>
                  <goals>
                      <goal>attach-artifact</goal>
                  </goals>
                  <configuration>
                      <artifacts>
                          <artifact>
                              <file>${project.build.directory}/geoserver.war</file>
                              <type>war</type>
                          </artifact>
                      </artifacts>
                  </configuration>
              </execution>
          </executions>
      </plugin-->
    </plugins>
  </build>

  <properties>
    <configId>minimal</configId>
    <configDirectory>${basedir}/../../geoserver/data</configDirectory>
    <webappSourceDirectory>${basedir}/src/main/webapp</webappSourceDirectory>
    <jetty.port>8080</jetty.port>
    <jetty.run.daemon>false</jetty.run.daemon>
  </properties>
  

  <profiles>
    <profile>
      <id>release</id>
      <properties>
        <configDirectory>${basedir}/../../geoserver/data</configDirectory>
        <configId>release</configId>
      </properties>
    </profile>
    <profile>
      <!-- GeoCat Live -->
      <id>live</id>
      <dependencies>
        <dependency>
          <groupId>org.geoserver.extension</groupId>
          <artifactId>gs-wps-core</artifactId>
          <version>${gs.version}</version>
        </dependency>
        <dependency>
          <groupId>org.geoserver.extension</groupId>
          <artifactId>gs-web-wps</artifactId>
          <version>${gs.version}</version>
        </dependency>
      </dependencies>
    </profile>
  </profiles>
  
  
  
</project>